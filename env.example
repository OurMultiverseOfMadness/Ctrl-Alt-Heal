# Ctrl-Alt-Heal Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# AWS Configuration
# =============================================================================
AWS_REGION=ap-southeast-1
AWS_PROFILE=default  # Optional: specify AWS profile to use

# =============================================================================
# Application Configuration
# =============================================================================
ENVIRONMENT=dev  # dev, staging, or production
PROJECT_NAME=CtrlAltHeal

# =============================================================================
# Amazon Bedrock Configuration
# =============================================================================
# Bedrock model for APAC region (Singapore)
BEDROCK_MODEL_ID=apac.amazon.nova-lite-v1:0
BEDROCK_MULTIMODAL_MODEL_ID=apac.amazon.nova-lite-v1:0
BEDROCK_REGION=ap-southeast-1  # Optional: defaults to AWS_REGION

# =============================================================================
# AWS Secrets Manager Configuration
# =============================================================================
# Secret names in AWS Secrets Manager (not the actual secrets)
TELEGRAM_SECRET_NAME=ctrl-alt-heal/dev/telegram/bot-token
SERPER_SECRET_NAME=ctrl-alt-heal/dev/serper/api-key

# =============================================================================
# AWS S3 Configuration
# =============================================================================
# S3 bucket names (will be created by CDK if they don't exist)
UPLOADS_BUCKET_NAME=ctrl-alt-heal-dev-uploads
ASSETS_BUCKET_NAME=ctrl-alt-heal-dev-assets

# =============================================================================
# AWS DynamoDB Configuration
# =============================================================================
# DynamoDB table names (will be created by CDK if they don't exist)
USERS_TABLE_NAME=ctrl_alt_heal_dev_user_profiles
CONVERSATIONS_TABLE_NAME=ctrl_alt_heal_dev_conversation_history
PRESCRIPTIONS_TABLE_NAME=ctrl_alt_heal_dev_prescriptions
IDENTITIES_TABLE_NAME=ctrl_alt_heal_dev_identities
FHIR_DATA_TABLE_NAME=ctrl_alt_heal_dev_fhir_data

# =============================================================================
# Logging Configuration
# =============================================================================
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# =============================================================================
# CDK Configuration (for deployment)
# =============================================================================
CDK_DEFAULT_ACCOUNT=  # Will be auto-detected from AWS CLI
CDK_DEFAULT_REGION=ap-southeast-1

# =============================================================================
# Development Configuration
# =============================================================================
# Optional: Override default values for development
# CDK_CONTEXT_JSON={"environment": "dev"}

# =============================================================================
# External API Configuration
# =============================================================================
# Note: These are stored in AWS Secrets Manager, not as environment variables
# The actual values should be set using the provided scripts:
# - python scripts/update_telegram_secret.py
# - python scripts/update_serper_secret.py

# Telegram Bot Token (set via script)
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Serper API Key (set via script)
# SERPER_API_KEY=your_serper_api_key_here

# =============================================================================
# Setup Instructions
# =============================================================================
# 1. Copy this file to .env: cp env.example .env
# 2. Update the values above with your actual configuration
# 3. Deploy the infrastructure: ./deploy-fargate.sh
# 4. Set up secrets using the provided scripts
# 5. Configure your Telegram bot webhook
# 6. Test the application

# =============================================================================
# Security Notes
# =============================================================================
# - Never commit the actual .env file to version control
# - Store sensitive values (API keys, tokens) in AWS Secrets Manager
# - Use different values for different environments (dev, staging, prod)
# - The .env file is for local development only
