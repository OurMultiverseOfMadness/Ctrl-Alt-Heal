# Core dependencies including strands-agents
# Excluding some heavy dependencies that might not be essential

# Pydantic and configuration
pydantic>=2.11.0
pydantic-settings>=2.10.0

# AI/ML dependencies - core strands packages
strands-agents>=1.6.0
strands-agents-tools>=0.2.5

# OpenTelemetry is disabled via environment variables to fix Lambda compatibility

# HTTP and networking
requests>=2.31.0

# Environment and utilities
python-dotenv>=1.1.0
pytz>=2023.3

# Utilities
werkzeug>=3.0.0

# Exclude heavy packages that might be optional:
# - numpy (if not directly used)
# - pillow (if not processing images)
# - lxml (if not parsing XML)
# - sympy (if not doing symbolic math)
