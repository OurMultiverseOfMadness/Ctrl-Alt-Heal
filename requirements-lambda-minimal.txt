# Ultra-minimal requirements for Lambda deployment
# Only essential dependencies for strands-agents

# Core Strands Agents
strands-agents==1.4.0

# Pydantic and configuration (required by strands-agents)
pydantic>=2.11.0
pydantic-settings>=2.10.0

# HTTP and networking (minimal set)
requests>=2.31.0
httpx>=0.28.0

# Environment and utilities (minimal)
python-dotenv>=1.1.0
pytz>=2023.3

# Calendar and scheduling (for medication reminders)
ics>=0.7.0

# Note: boto3 and botocore are provided by Lambda runtime
# Note: OpenTelemetry dependencies are included with strands-agents but should be handled by environment variables
